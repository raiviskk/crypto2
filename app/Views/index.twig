<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Crypto Pairs</title>
    <link href="/public/css/tailwind.css" rel="stylesheet">
    <!-- Include other CSS files or CDN links if necessary -->
</head>
<body class="bg-gray-100">

<div class="container mx-auto mt-8">
    <div class="border rounded-lg p-6 bg-white">
        {% include 'partial/navbar.twig' %}

        <h1 class="text-5xl font-extrabold text-blue-700 mb-4 border-b-4 border-blue-700 pb-2">Crypto Info</h1>


        <div class="overflow-x-auto">
            <table class="min-w-full bg-white border border-gray-300 rounded-lg overflow-hidden">
                <thead class="bg-gray-100">
                <tr>
                    <th class="py-2 px-4 border-b font-semibold text-left">Symbol</th>
                    <th class="py-2 px-4 border-b font-semibold text-left">Price (USD)</th>
                    <th class="py-2 px-4 border-b font-semibold text-left">24h Volume (USD)</th>
                    <th class="py-2 px-4 border-b font-semibold text-left">Volume Change (24h)</th>
                    <th class="py-2 px-4 border-b font-semibold text-left">Percent Change (24h)</th>
                </tr>
                </thead>
                <tbody>
                {% for pair in pairs %}
                    <tr class="{{ cycle(['bg-gray-50', ''], loop.index) }}">
                        <td class="py-2 px-4 border-b">{{ pair.symbol }}</td>
                        <td class="py-2 px-4 border-b">${{ pair.price|number_format(2) }}</td>
                        <td class="py-2 px-4 border-b">${{ pair.volume|number_format(2) }}</td>
                        <td class="py-2 px-4 border-b {{ pair.volumeChange > 0 ? 'text-green-500' : 'text-red-500' }}">{{ pair.volumeChange|number_format(2) }}%</td>
                        <td class="py-2 px-4 border-b {{ pair.percentChange > 0 ? 'text-green-500' : 'text-red-500' }}">{{ pair.percentChange|number_format(2) }}%</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

</body>
</html>
